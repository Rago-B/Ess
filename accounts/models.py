from django.db import models
# Using Django's built-in User model (django.contrib.auth.models.User)
# No custom model needed for the Login/Registration module.
# Future modules (Employee Profile, Attendance, Payroll) will extend via OneToOneField.
